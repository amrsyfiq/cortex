# Hoist nothing implicitly: packages must declare every dependency they import.
# This catches "phantom dependency" bugs where code works locally because a
# transitive dep happened to be hoisted, then breaks in CI/prod.
# We relax it for a few tools that expect to be hoisted.
auto-install-peers=true
strict-peer-dependencies=false
